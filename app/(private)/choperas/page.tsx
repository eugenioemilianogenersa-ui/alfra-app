export const dynamic = "force-dynamic";
export const metadata = { title: "Choperas | AlFra" };

import ChoperasClient from "./ChoperasClient";

export default function ChoperasPage() {
  return <ChoperasClient />;
}
